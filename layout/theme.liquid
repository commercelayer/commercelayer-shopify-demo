<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    {{ content_for_header }}
  </head>

  <body>
    <div class="container mx-auto p-6 max-w-screen-xl">
      {% render "header" %}
      <div class="flex flex-col md:flex-row">
        <div class="basis-1/2 mb-10 md:pr-10 lg:pr-20">
          <h1 class="text-2xl font-bold mb-4 lg:text-3xl">{{ template.name | prepend: 'titles.' | t }}</h1>
          <p>{{ template.name | prepend: 'hints.' | t }}</p>
        </div>
        <div class="basis-1/2">
          {{ content_for_layout }}
        </div>
      </div>      
      {% render "footer" %}
    </div>    
    {% render "commercelayer" %}
  </body>
 </html>