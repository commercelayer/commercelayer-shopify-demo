<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    {{ content_for_header }}
  </head>

  <body>
    <div class="container mx-auto p-6 max-w-screen-lg md:mt-10 lg:mt-20">
      {% render "header" %}
      {{ content_for_layout }}
      {% render "footer" %}
    </div>    
    {% render "commercelayer" %}
  </body>
 </html>